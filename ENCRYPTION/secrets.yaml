# if you want to ignore reading this yaml file, then you can directly set environment variables for below mentioned keys

# Azure secrets which we can fetch using this command :
# `az ad sp create-for-rbac --name <principal_name> --skip-assignment`

# to give vault permission to above principal use below command:
# `az keyvault set-policy --name <key-vault-name> --spn <AZURE_CLIENT_ID> --secret-permissions get `
AZURE_CLIENT_ID: #
AZURE_CLIENT_SECRET: #
AZURE_TENANT_ID: #
VAULT_NAME: screenshottest
SECRET_NAME: test3
KEY_NAME: test5